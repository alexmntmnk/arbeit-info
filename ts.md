
## 0) Общие предпосылки и тестовые данные

**Обязательные поля сообщения (минимум для тестов):**

* `docId` = ИД ДО (ключ идемпотентности)
* payload документа (контент)
* служебные поля (время, источник, версия схемы и т.п.)

**Набор подготовленных документов:**

* D1: валидный документ, новый `docId`
* D2: валидный документ, `docId` уже есть в БД
* D3: документ с “пограничным” размером (близко к лимитам)
* D4: документ с некорректной схемой (нет обязательного поля)
* D5: документ с некорректными значениями (например, отрицательная сумма / невалидная дата)
* D6: документ, провоцирующий ошибку сериализации/десериализации
* D7: документ, провоцирующий ошибку сохранения в БД (например, нарушение ограничения)
* D8: дубликат сообщения (один и тот же `docId`, отправленный дважды)
* D9: пачка 1000+ документов для нагрузочных сценариев

**Наблюдаемость (очень важно для проверок):**

* логи по результатам обработки (успех/ошибка/ретрай/отправка в TOPIK-01/TOPIK-02)
* метрики: количество обработанных, количество отправленных в TOPIK-01/02, ретраи, средняя задержка, время до успешного сохранения
* возможность выключать/включать БД или имитировать ошибки (фича-флаг/моки/iptables)

---

# 1) Обработка сообщений из FastTopic: ошибка → перенос в TOPIK-01

### TC-1.1 Успешная обработка FastTopic → сохранение в БД

**Предусловия:** БД доступна, документа с `docId=D1` нет.
**Шаги:**

1. Отправить D1 в FastTopic.
2. Дождаться обработки.
   **Ожидаемо:**

* D1 сохранён в БД.
* Сообщение **не** отправлено в TOPIK-01.
* Оффсет/ack в FastTopic подтверждён (сообщение не перечитывается повторно).

### TC-1.2 Ошибка обработки (валидация/бизнес-логика) → перенос в TOPIK-01

**Предусловия:** есть документ D4 или D5.
**Шаги:**

1. Отправить D4 (или D5) в FastTopic.
   **Ожидаемо:**

* В БД **не** появился новый документ.
* Исходное сообщение перенесено в TOPIK-01 (с сохранением `docId`, payload, и диагностикой причины, если предусмотрено).
* Сообщение в FastTopic подтверждено (чтобы не “молотило” бесконечно в FastTopic).

### TC-1.3 Ошибка десериализации/парсинга FastTopic → перенос в TOPIK-01 или TOPIK-02 (по требованиям)

**Предусловия:** D6 (битый формат).
**Шаги:**

1. Отправить D6 в FastTopic.
   **Ожидаемо (варианты):**

* Если по дизайну любые ошибки уходят в TOPIK-01: сообщение оказалось в TOPIK-01.
* Если “непарсится” и его нельзя корректно переиздать: сразу в TOPIK-02.
* В логах: причина “deserialization/parsing”.

### TC-1.4 Ошибка сохранения в БД (например, таймаут/недоступна) → перенос в TOPIK-01

**Предусловия:** имитация недоступности БД или forced timeout.
**Шаги:**

1. Отключить БД.
2. Отправить D1 в FastTopic.
   **Ожидаемо:**

* Сохранения в БД нет.
* Сообщение попало в TOPIK-01.
* Лог содержит классификацию ошибки как “DB write error”.

### TC-1.5 Ошибка частичного сохранения/транзакции → корректная компенсация

**Предусловия:** сценарий, где сервис пишет в несколько таблиц/коллекций.
**Шаги:**

1. Спровоцировать ошибку на втором шаге записи (например, constraint violation).
2. Отправить D7 в FastTopic.
   **Ожидаемо:**

* В БД нет “полудокумента” (либо транзакция откатилась, либо есть корректное состояние по правилам).
* Сообщение ушло в TOPIK-01.
* В логах: ошибка и отметка, что откат/компенсация отработали.

### TC-1.6 Повторная доставка сообщения из FastTopic (at-least-once) при сбое сервиса

**Предусловия:** D1 отправлен; сервис можно “уронить” между записью и ack.
**Шаги:**

1. Отправить D1 в FastTopic.
2. В момент после записи в БД, но до подтверждения оффсета — аварийно завершить сервис.
3. Запустить сервис снова.
   **Ожидаемо:**

* В БД один документ D1 (нет дублей).
* Повторная обработка не приводит к отправке в TOPIK-01.
* В логах: идемпотентность/проверка наличия (если реализована на этом этапе) или корректная обработка дубля.

---

# 2) Обработка TOPIK-01: поиск по docId, сохранение при отсутствии

### TC-2.1 TOPIK-01: документ отсутствует в БД → сохранить

**Предусловия:** D1 отсутствует в БД, лежит в TOPIK-01.
**Шаги:**

1. Поместить D1 в TOPIK-01.
2. Запустить обработчик TOPIK-01 (через шедулер или вручную).
   **Ожидаемо:**

* Выполнен поиск по `docId=D1`, не найден.
* Выполнено сохранение; D1 появился в БД.
* Сообщение в TOPIK-01 подтверждено/удалено.

### TC-2.2 TOPIK-01: документ уже есть в БД → не сохранять

**Предусловия:** D2 уже записан в БД.
**Шаги:**

1. Отправить D2 в TOPIK-01.
2. Запустить обработку.
   **Ожидаемо:**

* Поиск по `docId` нашёл запись.
* Новая запись **не** создаётся (ни дубль, ни обновление — если по ТЗ “не сохранять”).
* Сообщение подтверждено как обработанное (чтобы не крутилось бесконечно).
* Лог: “skip existing docId”.

### TC-2.3 TOPIK-01: гонка при параллельной обработке одного docId

**Предусловия:** 2 одинаковых сообщения с `docId=D8`, несколько consumer-ов/потоков.
**Шаги:**

1. Отправить два сообщения D8 в TOPIK-01 (или добиться редоставки).
2. Обеспечить параллельную обработку.
   **Ожидаемо:**

* В БД максимум 1 запись.
* Второй поток либо:

  * увидел запись при повторной проверке, либо
  * получил unique violation и корректно обработал это как “уже существует” (без ухода в TOPIK-02).
* Нет бесконечных ретраев.

### TC-2.4 TOPIK-01: поиск в БД падает (таймаут/ошибка) → запуск ретраев

**Предусловия:** БД отвечает ошибкой на SELECT.
**Шаги:**

1. Поместить D1 в TOPIK-01.
2. Спровоцировать сбой на чтении.
   **Ожидаемо:**

* Срабатывает механизм повторных попыток обработки TOPIK-01.
* Попытки считаются корректно (см. раздел 3).
* После восстановления БД документ сохраняется.

### TC-2.5 TOPIK-01: документ некорректен (валидация) → не пытаться бесконечно, уход в TOPIK-02

**Предусловия:** D4/D5 в TOPIK-01.
**Шаги:**

1. Поместить D4 в TOPIK-01.
2. Запустить обработку.
   **Ожидаемо:**

* Ошибка классифицируется как неретраимая (если предусмотрено) **или** всё равно ретраится ограниченно.
* После исчерпания попыток сообщение уходит в TOPIK-02.
* В БД не появляется запись.

---

# 3) Механизм N попыток + временные интервалы + защита от зацикливания

> Здесь важно проверить: **счётчик попыток**, **интервалы**, **что считается попыткой**, **что происходит при рестарте сервиса**, **не происходит ли “вечный пинг-понг” TOPIK-01 ↔ обработчик**.

### TC-3.1 Ретраи: успешное сохранение на K-й попытке (K < N)

**Предусловия:** БД недоступна первые 2 попытки, потом доступна. N=5, interval=например 1 мин.
**Шаги:**

1. Поместить D1 в TOPIK-01.
2. На 1-2 попытке держать БД недоступной.
3. На 3-й попытке включить БД.
   **Ожидаемо:**

* Попытки: 1,2 — ошибка; 3 — успех.
* Интервалы между попытками соответствуют настройке (допуск по времени оговорить: ±X секунд).
* После успеха документ в БД, сообщение не уходит в TOPIK-02.

### TC-3.2 Ретраи: исчерпание N попыток → отправка в TOPIK-02

**Предусловия:** БД постоянно недоступна или постоянная ошибка записи; N=3.
**Шаги:**

1. Поместить D1 в TOPIK-01.
2. Дождаться N попыток.
   **Ожидаемо:**

* Ровно N попыток обработки.
* После N-й неудачи сообщение отправлено в TOPIK-02.
* Сообщение из TOPIK-01 подтверждено/удалено (чтобы не крутилось дальше).
* В TOPIK-02 присутствует исходный payload + диагностическая информация + число попыток.

### TC-3.3 Проверка точного подсчёта попыток

**Шаги:**

1. Настроить N=2.
2. Вызвать обработку так, чтобы:

   * 1-я попытка падает на SELECT,
   * 2-я попытка падает на INSERT.
     **Ожидаемо:**

* Суммарно считалось 2 попытки обработки сообщения (а не 2 на SELECT и 2 на INSERT отдельно, если попытка — “полный цикл обработки”).
* Уход в TOPIK-02 после второй.

### TC-3.4 Защита от зацикливания: сообщение не должно бесконечно возвращаться в TOPIK-01

**Предусловия:** ошибка постоянно воспроизводится.
**Шаги:**

1. Вызвать ситуацию постоянной ошибки.
2. Наблюдать, что происходит после исчерпания попыток.
   **Ожидаемо:**

* Сообщение **не** переиздаётся снова в TOPIK-01 после достижения лимита.
* Финальная точка — TOPIK-02.

### TC-3.5 Сохранение счётчика попыток при рестарте сервиса

**Предусловия:** N=5; счётчик хранится либо в сообщении (header), либо в БД/кэше.
**Шаги:**

1. Поместить D1 в TOPIK-01, обеспечить 2 неудачные попытки.
2. Перезапустить сервис.
3. Дождаться продолжения.
   **Ожидаемо:**

* Попытка после рестарта считается как 3-я, а не снова 1-я (иначе возможен бесконечный цикл).
* Если по дизайну счётчик не переживает рестарт — это дефект/риск, фиксировать.

### TC-3.6 Корректность временных интервалов (fixed delay / exponential backoff)

**Шаги:**

1. Для N=4 записать фактические времена попыток (по логам/метрикам).
   **Ожидаемо:**

* Интервалы соответствуют конфигурации (например: 1м, 5м, 15м… или фиксированные).
* Нет “слипания” попыток из-за шедулера (например, шедулер каждые 10 секунд запускает и мгновенно тратит все попытки).

### TC-3.7 Нулевые/отрицательные/слишком большие параметры ретраев (валидация конфигурации)

**Шаги:**

1. Установить N=0.
2. Установить interval=0 или отрицательное.
3. Установить экстремально большой interval.
   **Ожидаемо:**

* N=0: либо сразу TOPIK-02, либо обработка отключена (ожидаемое поведение должно быть определено).
* interval<=0: сервис не должен входить в tight loop (CPU 100%).
* Большой interval: не переполнение таймеров, корректное планирование.

### TC-3.8 Переполнение/ограничения поля “attempt”

**Шаги:**

1. Смоделировать попытку записать attempt=2^31 (если int) или очень большое значение в headers.
   **Ожидаемо:**

* Не падает с overflow.
* Либо нормализует, либо сразу отправляет в TOPIK-02 как “corrupted metadata”.

---

# 4) Шедулер запуска обработки TOPIK-01

### TC-4.1 Шедулер запускается по расписанию и обрабатывает сообщения

**Предусловия:** TOPIK-01 содержит 10 сообщений.
**Шаги:**

1. Убедиться, что обработчик не запущен вручную.
2. Дождаться времени следующего тика шедулера.
   **Ожидаемо:**

* Обработка стартовала автоматически.
* Сообщения обработались (успех/skip/ретрай).

### TC-4.2 Наложение запусков шедулера (reentrancy)

**Предусловия:** обработка занимает дольше, чем период шедулера.
**Шаги:**

1. Настроить обработку так, чтобы один проход длился, например, 2 минуты.
2. Период шедулера 1 минута.
   **Ожидаемо:**

* Нет параллельных запусков, которые обрабатывают одни и те же сообщения (если нужен single-flight).
* Либо параллельность допустима, но нет дублей/гонок/потери оффсетов.

### TC-4.3 Выключение шедулера (feature flag) — сообщения не теряются

**Шаги:**

1. Отключить шедулер.
2. Положить сообщения в TOPIK-01.
3. Убедиться, что ничего не обрабатывается.
4. Включить шедулер обратно.
   **Ожидаемо:**

* После включения всё обработается корректно.
* Нет “просрочки” attempt логики (если завязано на timestamps).

### TC-4.4 Шедулер при пустом TOPIK-01

**Шаги:**

1. Убедиться, что TOPIK-01 пуст.
2. Дождаться тика.
   **Ожидаемо:**

* Нет ошибок/исключений.
* Нет лишней нагрузки на БД (например, бессмысленных запросов).

### TC-4.5 Шедулер при большом объёме (batch/limit)

**Предусловия:** TOPIK-01 содержит 100k сообщений.
**Шаги:**

1. Запустить шедулер.
   **Ожидаемо:**

* Обработка идёт порциями (если предусмотрено), сервис не падает по памяти.
* Соблюдаются лимиты времени/пакетов.
* Метрики отражают прогресс.

---

# 5) TOPIK-02: ручной разбор

### TC-5.1 Уход в TOPIK-02 содержит всю нужную информацию

**Шаги:**

1. Довести сообщение до исчерпания попыток.
2. Проверить сообщение в TOPIK-02.
   **Ожидаемо:**

* Есть `docId`, payload, причина последней ошибки, количество попыток, timestamps, корреляционный id (если есть).
* Формат сообщения соответствует ожиданиям потребителей ручного разбора.

### TC-5.2 TOPIK-02 не получает “успешные” или “skip existing” сообщения

**Шаги:**

1. Сценарий успеха (TC-2.1).
2. Сценарий “уже существует” (TC-2.2).
   **Ожидаемо:**

* В TOPIK-02 ничего не появляется.

### TC-5.3 Ошибка публикации в TOPIK-02 (Kafka недоступна) — не потерять сообщение

**Шаги:**

1. Спровоцировать исчерпание попыток.
2. В момент отправки в TOPIK-02 отключить брокер/топик.
   **Ожидаемо:**

* Сервис не “теряет” сообщение: либо повторит отправку в TOPIK-02, либо оставит в TOPIK-01 с признаком “готово к DLQ” и повторит позже.
* Никаких silent drops.

---

# 6) Идемпотентность, дубли, порядок, оффсеты

### TC-6.1 Дубликат в TOPIK-01: один docId — два сообщения

**Ожидаемо:** одна запись в БД, второе сообщение либо “skip”, либо корректно обработано как duplicate.

### TC-6.2 Сообщение из FastTopic попало в TOPIK-01, но параллельно уже было сохранено из FastTopic

**Шаги:**

1. Имитировать ситуацию: FastTopic обработчик записал в БД, но из-за ошибки “после записи” всё равно отправил в TOPIK-01.
2. TOPIK-01 обработчик запускается.
   **Ожидаемо:**

* TOPIK-01 выполнит поиск и **не** будет повторно сохранять.
* Не уйдёт в TOPIK-02.

### TC-6.3 Нарушение порядка сообщений для одного docId (если есть версии документа)

Если payload может обновляться, проверьте:

* более новая версия не должна быть перетёрта старой (если применимо).
  (Если по ТЗ “не сохранять, если docId есть” — тогда обновлений вообще нет, и это тоже нужно зафиксировать как ожидаемое поведение.)

### TC-6.4 Семантика ack/commit: “обработал → подтвердил”

**Шаги:**

1. Спровоцировать падение сервиса до и после commit оффсета.
   **Ожидаемо:**

* Нет потери сообщений.
* Допустимы дубли (at-least-once), но нет дублей в БД.

---

# 7) Негативные и “грязные” данные

### TC-7.1 Пустой docId / null docId

**Ожидаемо:**

* Сообщение не должно попадать в бесконечный ретрай.
* Скорее всего сразу TOPIK-02 (неретраимая ошибка данных).

### TC-7.2 Очень длинный docId (максимум поля)

**Ожидаемо:** не падает, корректно ищет/пишет или валидирует и отправляет в TOPIK-02.

### TC-7.3 Очень большой payload (близко к лимиту сообщения/БД)

**Ожидаемо:** корректная обработка или контролируемая ошибка с уходом в TOPIK-01/02.

### TC-7.4 Некорректная кодировка/символы

**Ожидаемо:** либо нормальная обработка Unicode, либо понятная ошибка без падения сервиса.

### TC-7.5 Несовместимая версия схемы сообщения

**Ожидаемо:** контролируемое поведение (reject → TOPIK-02, либо преобразование).

---

# 8) Ошибки инфраструктуры

### TC-8.1 TOPIK-01 недоступен при попытке переноса из FastTopic

**Шаги:**

1. Отключить брокер/запретить запись в TOPIK-01.
2. В FastTopic дать сообщение, которое упадёт по БД.
   **Ожидаемо:**

* Сервис не должен “съесть” сообщение молча.
* Либо: не подтверждать оффсет FastTopic (чтобы перечитать позже), либо писать в локальный fallback (если предусмотрено).
* В логах явная критическая ошибка.

### TC-8.2 TOPIK-01: чтение из топика падает

**Ожидаемо:** шедулер/консюмер не падает навсегда, есть ретрай подключения.

### TC-8.3 БД флапает (то есть доступна/недоступна)

**Ожидаемо:** ретраи работают, нет лавинообразной нагрузки (thundering herd).

---

# 9) Нагрузочные/стабильность/деградация

### TC-9.1 Массовые ошибки FastTopic → лавина в TOPIK-01

**Шаги:**

1. Отключить БД.
2. Отправить 10k сообщений в FastTopic.
3. Включить БД.
   **Ожидаемо:**

* В TOPIK-01 оказалось 10k сообщений.
* После включения БД — постепенное разгребание, без падения сервиса.
* Нет потерь, нет дублей в БД.

### TC-9.2 TOPIK-01 backlog + шедулер: ограничение скорости

**Ожидаемо:** есть rate limit/batch size; сервис не умирает по памяти/коннектам к БД.

### TC-9.3 Длительная работа (soak test 8–24 часа)

**Ожидаемо:** нет утечек памяти, счётчики ретраев не “плывут”, стабильные метрики.

---

# 10) Безопасность от “зацикливания” и неправильной маршрутизации

### TC-10.1 Сообщение из TOPIK-01 при ошибке НЕ должно снова публиковаться в TOPIK-01 как “новое”

**Ожидаемо:** ретраи происходят в рамках механизма попыток, а не через повторную “переотправку в тот же топик” без контроля.

### TC-10.2 Сообщение из FastTopic при ошибке в TOPIK-01 обработчике не должно внезапно уходить обратно в FastTopic

**Ожидаемо:** маршрутизация строго по ТЗ.

### TC-10.3 Защита от “poison pill” (сообщение всегда падает)

**Ожидаемо:** уходит в TOPIK-02, не блокирует партицию навсегда.

---

## Рекомендуемая структура для оформления каждого тест-кейса (в вашем TMS)

Для единообразия, к каждому кейсу добавьте поля:

* **ID / Название**
* **Цель**
* **Предусловия**
* **Тестовые данные**
* **Шаги**
* **Ожидаемый результат**
* **Проверки в логах/метриках**
* **Постусловия** (чистка БД, очистка топиков)
* **Тип** (functional / negative / load / reliability)

---

Если скажете:

1. какой брокер (Kafka/Rabbit/другое),
2. как хранится счётчик попыток (header/БД/redis),
3. какие именно интервалы ретраев (fixed/exponential) и N,
   я дополню список **точечными кейсами под вашу реализацию** (например, конкретные проверки headers, ключа партиционирования, точных таймингов, и семантики commit).
